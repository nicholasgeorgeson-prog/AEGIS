# AEGIS Development Dependencies
# Install: pip install -r dev-requirements.txt
# These are NOT required for production — development/CI only.

# Linter — fast Python linter (replaces flake8 + isort + pyupgrade)
ruff>=0.4.0

# Security auditing — scans installed packages for known vulnerabilities
pip-audit>=2.7.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
